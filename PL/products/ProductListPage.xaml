<Page x:Class="PL.products.ProductListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PL.products"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductListPage" >
    <!--<Grid Name="MainGrid" Background="#FFDEDBD2" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="210*"/>
            <RowDefinition Height="79*"/>
        </Grid.RowDefinitions>
        <Grid Name="UpGrid" Height="auto" Grid.Row="0" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions >
            <Label Grid.Column="2" Content=":סנן לפי" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#FF49A078" Foreground= "White" FontFamily="Segoe UI Variable Display Semibold" Width="160" Height="46" VerticalContentAlignment="Center" FontSize="18" HorizontalContentAlignment="Center"/>
            <ComboBox x:Name="categorySelector" HorizontalAlignment="Center" Grid.Column="0" SelectedItem="{Binding Category}" VerticalAlignment="Center" Width="320" Height="45" SelectionChanged="categorySelector_SelectionChanged">
                <ComboBoxItem Content="קודש" />
                <ComboBoxItem Content="הסטוריה" />
                <ComboBoxItem Content="ילדים" />
                <ComboBoxItem Content="רומנים" />
                <ComboBoxItem Content="עיוני" />
            </ComboBox>
            <ComboBox x:Name="priceSelector" HorizontalAlignment="Center" Grid.Column="1" VerticalAlignment="Center" Width="320" Height="45" Background="#FF49A078" BorderBrush="#FF49A078" OpacityMask="#FF49A078" SelectionChanged="priceSelector_SelectionChanged" >
                <ComboBoxItem Content="₪50 עד" />
                <ComboBoxItem Content="₪100 עד" />
                <ComboBoxItem Content="₪150 עד" />
                <ComboBoxItem Content="₪200 עד" />
                <ComboBoxItem Content="₪200+" />
            </ComboBox>
        </Grid>
        <Grid Name="DownGrid" Height="auto" Grid.Row="1" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="46*"/>
                <ColumnDefinition Width="43*"/>
            </Grid.ColumnDefinitions>
        </Grid>

        <DataGrid x:Name="productForListDataGrid"  AutoGenerateColumns="False" Background="#DCE1DE" EnableRowVirtualization="True" RowDetailsVisibilityMode="VisibleWhenSelected" ItemsSource="{Binding}" Grid.Row="1" Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="nameColumn" Binding="{Binding Name}" Header="Name" Width="200"/>
                <DataGridTextColumn x:Name="idColumn" Binding="{Binding ID}" Header="ID" Width="200"/>
                <DataGridTextColumn x:Name="categoryColumn" Binding="{Binding Category}" Header="Category" Width="200"/>
                <DataGridTextColumn x:Name="priceColumn" Binding="{Binding Price}" Header="Price" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="updateProduct_Button" Grid.Row="2" Content="עדכון מוצר" HorizontalAlignment="Left" Margin="500,0,0,0" VerticalAlignment="Top" Click="update_Click" Padding="1" Background="#FF216869" Foreground="#FFDCE1DE" Width="200" Height="36" FontSize="18"/>
        <Button x:Name="AddProduct_Button" Grid.Row="2" Content="הוספת מוצר חדש" HorizontalAlignment="Left"  Margin="100,0,0,0" VerticalAlignment="Top" Click="Add_Click" Padding="1" Background="#FF216869" Foreground="#FFDCE1DE" Width="200" Height="36" FontSize="18"/>
    </Grid>-->
</Page>
