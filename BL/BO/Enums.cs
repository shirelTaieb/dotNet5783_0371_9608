namespace BO
{

    public enum Category { Children, Holy, Theoretical, History, Romans, None };
    public enum HebCategory { ילדים, קודש, עיוני, היסטוריה, רומנים, הכל };
    public enum OrderStatus { confirm, sent, deliveried };
    

}
